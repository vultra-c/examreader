<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.silenthong.kdreader">

    <!-- 权限声明（与弦电子书一致） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 关键：声明可查询的宿主应用包名
         XMS Wearable SDK 通过 IPC 与以下两个宿主应用通信来发现手环设备：
         - com.xiaomi.wearable (小米穿戴)
         - com.mi.health (小米运动健康)
         在 Android 11+ (API 30+) 上，如果没有此 <queries> 声明，
         nodeApi.connectedNodes 会静默返回空列表，导致"设备找不到" -->
    <queries>
        <package android:name="com.xiaomi.wearable" />
        <package android:name="com.mi.health" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
